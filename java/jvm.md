# 底层

jvm大体上分为两个区域，堆和栈

1. 堆：线程隔离
   存对象
2. 元空间：线程共享
   常量。静态变量
3. 虚拟机栈（java栈）：
4. 本地方法栈:native方法，调用其他语言
5. 程序计数器

# 垃圾回收

## **G1**

# 线程

## 线程池

- 核心线程数<当前线程数时：新线程优先加入阻塞队列，而不是创建新线程。
- 当前线程数>核心线程数+阻塞队列时：创建新线程
- 当前线程数>最大线程数+阻塞队列时：任务拒绝策略
